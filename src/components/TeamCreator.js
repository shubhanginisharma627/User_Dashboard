import React from 'react';
import './css/TeamCreator.css';

const TeamCreator = ({ users, onAddToTeam }) => {
  return (
    <div className="team-creator">
      {/* Logic to select users and add to team */}
    </div>
  );
}

export default TeamCreator;
